services:
  calculatrice-api:
    build:
      context: .
      dockerfile: Dockerfile
    image: calculatrice-api:latest
    container_name: calculatrice-api
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    restart: unless-stopped
